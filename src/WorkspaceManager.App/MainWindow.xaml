<Window x:Class="WorkspaceManager.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Desktop Workspace Manager">
    <Grid x:Name="Root" Padding="40,28,40,20" RowSpacing="20" KeyDown="OnKeyDown">
        <Grid.Resources>
            <Style x:Key="SecondaryTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}"/>
            </Style>
            <Style x:Key="MonitorPanelStyle" TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource LayerFillColorDefaultBrush}"/>
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            </Style>
            <Style x:Key="WindowCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel Spacing="5">
                <TextBlock x:Name="AppTitle" Text="Desktop Workspace Manager" FontSize="30" FontWeight="SemiBold"/>
                <TextBlock x:Name="Subtitle" Text="All desktops and monitors, one view." Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button x:Name="EnterButton" Content="Enter desktop" Click="EnterDesktop_Click" Style="{StaticResource AccentButtonStyle}"/>
                <Button x:Name="RefreshButton" Content="Refresh" Click="Refresh_Click"/>
                <Button x:Name="SettingsButton" Content="Settings" Click="Settings_Click"/>
                <Button x:Name="HideButton" Content="Hide" Click="Hide_Click" ToolTipService.ToolTip="Esc · Hide to tray"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" ColumnSpacing="14">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" HorizontalScrollMode="Enabled">
                <StackPanel x:Name="DesktopStrip" Orientation="Horizontal" Spacing="12" Padding="0,0,0,8"/>
            </ScrollViewer>
            <Button x:Name="CreateButton" Grid.Column="1" Content="＋ New desktop" VerticalAlignment="Top" Height="80" Click="CreateDesktop_Click"/>
        </Grid>
        <InfoBar x:Name="Notice" Grid.Row="2" IsOpen="False" IsClosable="True"/>
        <ScrollViewer x:Name="ContentScroll" Grid.Row="3" Background="Transparent" AllowDrop="True"
            HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
            HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" ViewChanged="ContentScroll_ViewChanged">
            <Grid x:Name="MonitorPanels" Background="Transparent" ColumnSpacing="20" Padding="0,0,12,12"/>
        </ScrollViewer>
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock x:Name="Hint" Text="Drag a window to a monitor or desktop · hover a desktop for cross-monitor targets · right-click for more" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <TextBlock x:Name="ShortcutHint" Grid.Column="1" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </Grid>
    </Grid>
</Window>
